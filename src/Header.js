import './App.css';

import React from 'react';

const Header = () => {
    return(
        <header className="App-header">
            <h2>Bookstore</h2>
        </header>
    );
};

export default Header;
